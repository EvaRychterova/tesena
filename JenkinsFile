pipeline {
	agent any
	environment {
	CI = 'true'
	}
	stages {
		stage('Install') {
      steps {
        cmd 'npm install'
      }
		 }
		 stage('Run Build') {
		 steps {
		    cmd 'npm run build'
		    }
		 }
	}
}
