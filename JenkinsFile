pipeline {
	agent any
	environment {
	CI = 'true'
	}
	stages {
		stage('Install') {
      steps {
        sh 'npm install'
      }
		 }
		 stage('Run Build') {
		 steps {
		    sh 'npm run build'
		    }
		 }
	}
}
